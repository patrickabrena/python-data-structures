#notes on tuples
my_tuple = (1, "strings", 4.5, True)
#tuple can accept any mix of data types. 
#tuples are like lists that have values that are immutable

#can access a value at certain index
print(my_tuple[1])

#can look for number of occurences of that value within tuple
print(my_tuple.count(4.5))